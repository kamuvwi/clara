using System;

namespace Helpers
{
    public class Array
    {
        public static dynamic ArrayLength(dynamic value)
        {
            var valueCount = value.Length;
            return valueCount;
        }

    }
}
